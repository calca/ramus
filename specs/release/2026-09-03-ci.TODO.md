# CI: test automatici + build di release

Stato: `test.yml` implementato. `release.yml` resta bloccato sulla
spec di firma (`2026-09-03-firma-notarizzazione.TODO.md`), non
scritto ancora — non c'è nulla da tenere "pronto ma disattivato" senza
sapere come sarà configurata la firma.

## Motivazione

`.github/workflows/` non esiste: niente gira automaticamente su push
o pull request. Ogni verifica (`cargo test`, `cargo clippy`, `npm run
typecheck`, `npm run test`) è fatta a mano, task per task — funziona
in una sessione di lavoro seguita passo passo, ma non protegge da una
regressione che sfugge a una verifica manuale, né permette a un
collaboratore futuro di sapere se il proprio branch è a posto senza
chiedere.

## Due workflow separati

### 1. `test.yml` — nessuna domanda aperta, implementabile subito

Su ogni push e pull request verso `main`:
- `cargo test`, `cargo clippy --all-targets -- -D warnings`, `cargo
  fmt --all -- --check` (stessa identica checklist di CLAUDE.md).
- `npm run typecheck`, `npm run test` (vitest).
- Matrice minima: **solo Ubuntu** per questo workflow — i test Rust
  del core sono già cross-platform per costruzione (`tempfile`,
  percorsi via `std::path`), non serve tripla matrice per test unitari
  che non toccano funzionalità OS-specifiche (mobile `cfg(mobile)`,
  ancora non compilato/testato nemmeno localmente su un target reale —
  vedi `specs/release/2026-09-03-verifica-cross-platform.TODO.md`).

### 2. `release.yml` — bloccato sulla spec di firma

Su un tag `v*`: build della matrice macOS/Windows/Linux via
`tauri-apps/tauri-action` (l'action ufficiale, stesso principio di
"solo dipendenze ufficiali" già seguito per i plugin Tauri), upload
degli asset su una GitHub Release. **Non implementabile in modo
sensato prima della spec di firma**: pubblicare build non firmate
automaticamente non risolve nulla, anzi restituisce artefatti che
Gatekeeper/SmartScreen bloccano comunque — l'automazione della build
va di pari passo con la firma, non prima.

## Modifiche

`.github/workflows/test.yml` (nuovo file), due job paralleli:

- **`rust`**: installa i prerequisiti Linux di Tauri (`libwebkit2gtk-4.1-dev`,
  `libgtk-3-dev`, `libayatana-appindicator3-dev`, `librsvg2-dev`,
  `patchelf` — necessari perché `cargo test`/`clippy --all-targets`
  compilano anche `src-tauri`, membro del workspace), poi `cargo fmt
  --check`, `cargo clippy --all-targets -D warnings`, `cargo test` —
  stessa identica checklist di CLAUDE.md.
- **`frontend`**: `npm ci` (non `npm install`: usa il lockfile
  com'è, fallisce se è disallineato — il comportamento giusto in CI),
  `npm run typecheck`, `npm run test`.

Due nuove GitHub Action non ufficiali ma standard de facto per CI
Rust, motivate qui invece che nel commit essendo infrastruttura non
codice applicativo: `dtolnay/rust-toolchain` (installazione toolchain
con componenti clippy/rustfmt — l'alternativa storica `actions-rs/*`
non è più mantenuta) e `Swatinem/rust-cache` (cache della cartella
`target/`, evita di ricompilare l'intero workspace a ogni push).
`actions/checkout`/`actions/setup-node` sono ufficiali GitHub.

`release.yml` non scritto: nessun contenuto sensato da mettere finché
la spec di firma non stabilisce come firmare gli artefatti.

## Domande aperte

Nessuna per `test.yml`. `release.yml` eredita le domande già aperte
nella spec di firma (credenziali, dove ospitarle come GitHub Actions
secrets).

## Fuori scope

- Deploy automatico di documentazione/sito: non esiste un sito per
  Ramus oltre al README.
- Badge di stato CI nel README: aggiunta cosmetica banale, si può fare
  in un secondo momento senza bisogno di una spec a parte.

## Verifica

YAML validato, tutti i comandi verificati verdi localmente, **e
verificato per davvero**: il primo push ha fatto girare il workflow
su un runner Ubuntu reale di GitHub Actions (run 33811162752), entrambi
i job verdi (`rust` 7m30s, `frontend` 37s) — inclusa la lista dei
pacchetti apt per Tauri, che non era verificabile localmente su
macOS. `release.yml`: non verificabile, non scritto.
